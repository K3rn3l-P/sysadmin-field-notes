# Template for /etc/postfix/sasl_passwd — this is NOT the real file.
# Copy this line (or generate it with the script), replace it with your own
# real credentials, and save it as /etc/postfix/sasl_passwd (never inside this
# repo folder — see .gitignore: real .env/sasl_passwd files must never be
# committed).

[smtp.gmail.com]:587    your-email@gmail.com:your-app-password
